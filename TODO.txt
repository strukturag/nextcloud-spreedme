TODO:

For TODOs not listed in here, search for them via:
  $ grep -r --exclude-dir=".git" --exclude="TODO.txt" "TODO" .

- Move config stuff from helper.php to own class
- Move owncloud.js, owncloud.js should be an empty file (for extra folder backwards-compatibility)
- Unit Tests
- Add adminPageController for admin stuff
